import { Column, Columns } from "../..";

const Footer: React.FC = () => {
  return (
    <footer className="footer">
      <Columns>
        <Column>Things to do!</Column>
      </Columns>
    </footer>
  );
};

export default Footer;
